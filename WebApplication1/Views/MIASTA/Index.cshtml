@model IEnumerable<EFSecond.Models.MIASTA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
</p>
@using (Html.BeginForm())
{
    <p>
        Wyszukaj: @Html.TextBox("SearchingString")
        <input type="submit" class="btn btn-default" value="Search" />
    </p>
}

<table class="table">
    <tr>
        <th>
            @Html.ActionLink("ID_MIASTA", "Index", new { sortOrder = ViewBag.IDSortParam })
        </th>
        <th>
            @Html.ActionLink("NAZWA_MIASTA", "Index", new { sortOrder = ViewBag.NAZWAMIASTASortParm })
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID_MIASTA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NAZWA_MIASTA)
            </td>
            <td>
                @if (User.Identity.IsAuthenticated)
                { <div>
                    @Html.ActionLink("Edit", "Edit", new { id = item.ID_MIASTA }) |
                    @Html.ActionLink("Details", "Details", new { id = item.ID_MIASTA })|

                    @Html.ActionLink("Delete", "Delete", new { id = item.ID_MIASTA })
                </div>
                }

                else
                {
                    <div>
                        @Html.ActionLink("Details", "Details", new { id = item.ID_MIASTA })
                    </div>
                }
</td>
        </tr>
    }

</table>
