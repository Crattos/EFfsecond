@model IEnumerable<EFSecond.Models.DRUZYNY>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm())
{
    <p>
        Wyszukaj: @Html.TextBox("SearchingString")
        <input type="submit" class="btn btn-default" value="Search"/>
    </p>
}

<table class="table">
    <tr>
        <th>
            @Html.ActionLink("ID_DRUZYNY", "Index", new { sortOrder = ViewBag.IDSortParam })
</th>
        <th>
            @Html.ActionLink("ID_MIASTA", "Index", new { sortOrder = ViewBag.IDMiastaSortParm })
        </th>
        <th>
            @Html.ActionLink("NAZWA_DRUZYNY", "Index", new { sortOrder = ViewBag.NAZWADRUZYNYSortParm })
        </th>
        <th>
            @Html.ActionLink("PUNKTY", "Index", new { sortOrder = ViewBag.PUNKTYSortParm })

        </th>
        <th>
            @Html.ActionLink("BRAMKI", "Index", new { sortOrder = ViewBag.BRAMKISortParm })
        </th>
        <th>
            @Html.ActionLink("ILOSC_SPOTKAN", "Index", new { sortOrder = ViewBag.ILOSCSPOTKANSortParm })
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        <th>
            @Html.DisplayFor(modelItem => item.ID_DRUZYNY)
        </th>
        <td>
            @Html.DisplayFor(modelItem => item.ID_MIASTA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NAZWA_DRUZYNY)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PUNKTY)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BRAMKI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ILOSC_SPOTKAN)
        </td>
        <td>
            @if (User.Identity.IsAuthenticated)
                { <div>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID_DRUZYNY }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID_DRUZYNY })|

                @Html.ActionLink("Delete", "Delete", new { id = item.ID_DRUZYNY })
            </div>
            }

            else
            {
                <div>
                    @Html.ActionLink("Details", "Details", new { id = item.ID_DRUZYNY })
                </div>
            }
        </td>
    </tr>
}

</table>
