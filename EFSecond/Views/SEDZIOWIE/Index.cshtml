@model IEnumerable<EFSecond.Models.SEDZIOWIE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@using (Html.BeginForm())
{
    <p>
        Wyszukaj: @Html.TextBox("SearchingString")
        <input type="submit" class="btn btn-default" value="Search" />
    </p>
}
<table class="table">
    <tr>
        <th>
            
            @Html.ActionLink("ID_SEDZI", "Index", new { sortOrder = ViewBag.IDSortParm })


        </th>
        <th>
            @Html.ActionLink("IMIE_SEDZI", "Index", new { sortOrder = ViewBag.IMIESEDZISortParm })


        </th>
        <th>
            @Html.ActionLink("NAZWISKO_SEDZI", "Index", new { sortOrder = ViewBag.NAZWISKOSEDZISortParm })


        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <th>
            @Html.DisplayFor(modelItem => item.ID_SEDZI)
        </th>
        <td>
            @Html.DisplayFor(modelItem => item.IMIE_SEDZI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NAZWISKO_SEDZI)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_SEDZI }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_SEDZI }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_SEDZI })
        </td>
    </tr>
}

</table>
